# Target signature

```python
{{ signature_line }}
{% if docstring -%}
    """{{ docstring }}"""
{%- endif %}
```

# Return type schema (JSON Schema)

```json
{{ return_schema_json }}
```

# Tool surface

{% if tools_descriptions -%}
{{ tools_descriptions }}
{%- else -%}
(no tools — the body must be self-contained)
{%- endif %}

# Inputs for this call

```json
{{ inputs_json }}
```

{% if archive_neighbors -%}
# Style reference — prior archived bodies (not callable)

{% for entry in archive_neighbors -%}
## {{ entry.qualname }}

{{ entry.reasoning }}

```python
{{ entry.body }}
```

{% endfor %}
{%- endif %}

{% if repair -%}
# REPAIR CONTEXT

The prior body raised. Revise it.

## Prior body

```python
{{ repair.prior_body }}
```

## Failure

- Type: `{{ repair.exception_type }}`
- Message: `{{ repair.exception_message }}`
{% if repair.exception_args -%}
- Structured args: `{{ repair.exception_args_json }}`
{%- endif %}
{% if repair.failing_line is not none -%}
- Line: {{ repair.failing_line }}
{%- endif %}

{% if repair.trace_excerpt -%}
## Last tool/helper calls before failure

{% for t in repair.trace_excerpt -%}
- {{ t }}
{% endfor %}
{%- endif %}

{% if repair.prior_attempts -%}
## Prior repair attempts: {{ repair.prior_attempts }}
{%- endif %}

Emit a revised body. Be more defensive where the exception implicates, but do not change the signature and do not add libraries outside the sandbox allow-list.
{%- endif %}
