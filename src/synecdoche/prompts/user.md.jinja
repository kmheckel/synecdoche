# Operator: {{ operator }}

# Target signature

```python
{{ signature_line }}
{% if docstring -%}
    """{{ docstring }}"""
{%- endif %}
```

# Return type schema (JSON Schema)

```json
{{ return_schema_json }}
```

# Tool surface

{% if tools_descriptions -%}
{{ tools_descriptions }}
{%- else -%}
(no tools — the body must be self-contained)
{%- endif %}

# Inputs for this call

```json
{{ inputs_json }}
```

{% if parents -%}
# Parent{{ 's' if parents|length > 1 else '' }}

{% for p in parents -%}
## Parent {{ p.index }}

{{ p.reasoning }}

```python
{{ p.body }}
```

{% endfor -%}
{%- endif %}

{% if signals -%}
# Signals against parent 1

{% for s in signals -%}
- **{{ s.kind }}** (weight {{ '%+.2f'|format(s.weight) }}): {{ s.content }}
{%- if s.data_json and s.data_json != '{}' %}
  structured: `{{ s.data_json }}`
{%- endif %}
{% endfor %}
{%- endif %}

{% if neighbors -%}
# Style reference — champions of other signatures (not callable)

{% for entry in neighbors -%}
{{ entry.reasoning }}

```python
{{ entry.body }}
```

{% endfor -%}
{%- endif %}

{% if operator == 'spawn' -%}
Emit the first body for this signature.
{%- elif operator == 'mutate' -%}
Emit a descendant of parent 1. Resolve every hard signal; move in the direction of the soft signals; change nothing the signals do not implicate. Do not change the signature and do not add libraries outside the sandbox allow-list.
{%- elif operator == 'cross' -%}
Emit one body recombining the strengths of both parents. Do not change the signature and do not add libraries outside the sandbox allow-list.
{%- endif %}
